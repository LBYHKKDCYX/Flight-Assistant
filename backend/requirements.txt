Flask>=2.0.1,<3.0
flask-cors>=5.0
requests>=2.26.0,<3.0
flasgger>=0.9.5,<1.0

# 生产环境 WSGI 服务器（Linux 用 gunicorn，Windows 用 waitress）
# waitress>=2.1
gunicorn>=20.1

# 对于Python 3.9以下版本，需要安装pytz
# pytz>=2021.3